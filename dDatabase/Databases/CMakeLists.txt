set(DDATABASE_DATABASES_SOURCES)

add_subdirectory(MySQL)

foreach(file ${DDATABSE_DATABSES_MYSQL_SOURCES})
	set(DDATABASE_DATABASES_SOURCES ${DDATABASE_DATABASES_SOURCES} "MySQL/${file}")
endforeach()

set(DDATABASE_DATABASES_SOURCES ${DDATABASE_DATABASES_SOURCES} PARENT_SCOPE)
